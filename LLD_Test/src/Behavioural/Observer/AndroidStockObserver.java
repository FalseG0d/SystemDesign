package Behavioural.Observer;

public class AndroidStockObserver extends StockObserver {
    AndroidStockObserver(String user, StockObservable stockObservable) {
        super(user, stockObservable);
    }
}
